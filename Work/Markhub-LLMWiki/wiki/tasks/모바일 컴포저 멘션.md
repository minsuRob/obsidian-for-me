---
유형: 일감
상태: 진행중
우선순위: 최우선
생성일: 2026-07-03
갱신일: 2026-07-03
출처:
  - "[[2026-07-03]]"
---

# 모바일 컴포저 멘션 (사람 + 일감)

## 배경
모바일 컴포저에 **사람 언급창**(@)과 **todo/note 일감 언급**을 추가한다. 목표 경험은 웹의 "2번 느낌"(= `@` 멘션 메뉴). 사람 언급은 **mobile native로 개발**.

- 대상 파일: `apps/front/features/mobile/message/components/mobile-mark/MobileMarkComposer.tsx` (+ `.ios.tsx`, `.android.tsx`)
- 공유 뿌리: [[멘션 인프라 (Lexical)]]

## 현황
- **핵심 제약**: 모바일 컴포저는 평문 React Native `TextInput`(문자열 state)이라, 노드 기반 Lexical 멘션을 그대로 못 쓴다 → 멘션 UI를 **새로 만들어야** 함.
- iOS/Android는 각각 `@expo/ui/swift-ui`, `@expo/ui/jetpack-compose` 래퍼 존재 → 네이티브 팝오버 구현 여지 있음.
- 데이터/검색 로직은 재사용 가능: `useWorkspaceMembers`(사람), `useBucketItems`·`useSearchMyBucketItemsWithSemantic`(일감), `MentionSuggestion` 타입, `toBucketSuggestion`.

## 구현 방향 (결정 ✅ 2026-07-03: 옵션 B 완전 네이티브)
- **채택: B. 완전 네이티브 팝오버** — iOS SwiftUI Picker + Android Compose BottomSheet. 사람 언급을 native로 개발한다는 지시에 정확히 부합.
- 반려: A(WebView 임베드 — native 지시와 거리), C(하이브리드 — 초기 속도는 좋으나 최종 목표가 native).
- 이원화 부담(iOS/Android 각각 구현·테스트)은 감수. 데이터/검색 로직은 공통 훅으로 공유.

## 만들 조각 (옵션 B 기준)
- `useMobileMentionSuggestions.ts` — `@` 감지 + 필터 + 서버검색 통합 (공통 로직).
- (native) `MobileMentionPopover.ios.tsx` (SwiftUI Picker) / `MobileMentionSheet.android.tsx` (Compose ModalBottomSheet) — 섹션: Members / Data Bucket, 항목에 avatar·badge·secondary.
- `@` 감지: 평문 TextInput의 커서 앞 텍스트에서 `@` 파싱 → 로컬(useWorkspaceMembers/useBucketItems) 즉시 + 250ms 디바운스 후 서버(useSearchMyBucketItemsWithSemantic).
- 전송 직렬화: `@user`/`#task` + mentions 메타(userId/bucketItemId, position) — 기존 `useMarkComposerSubmit.ts` 확장.

## 타임라인
- 2026-07-03: MAKi 코드 조사. 컴포저가 평문 TextInput이라 멘션 UI 신규 필요 확인. 옵션 A/B/C 정리 (출처: [[2026-07-03]]).
- 2026-07-03: **옵션 B(완전 네이티브) 채택** 결정.

## 관련
- [[멘션 인프라 (Lexical)]]
- [[웹 assign 일감 언급]]
