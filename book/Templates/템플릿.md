<%*
let title = tp.frontmatter["제목"] ?? "";
let author = tp.frontmatter["저자"] ?? "";
let field = tp.frontmatter["분야"] ?? "소설";
let status = tp.frontmatter["상태"] ?? "읽는중";
let pageCount = tp.frontmatter["페이지수"] ?? "";
let rating = tp.frontmatter["평가"] ?? "";
const today = tp.date.now("YYYY-MM-DD");
let published = tp.frontmatter["발행일"] ?? today;
let startedReading = tp.frontmatter["읽기 시작한 날짜"] ?? today;
let finishedReading = tp.frontmatter["완독 날짜"] ?? today;

if (!title) {
  title = await tp.system.prompt("제목") ?? "";
}

if (!author) {
  author = await tp.system.prompt("저자") ?? "";
}

const fileName = title
  .replace(/[\\/:*?"<>|#^[\]]/g, "")
  .replace(/\s+/g, " ")
  .trim();
const currentFile = app.workspace.getActiveFile();
if (fileName && currentFile && currentFile.basename !== fileName) {
  const folder = currentFile.parent?.path ?? "";
  const newPath = `${folder ? folder + "/" : ""}${fileName}.md`;
  await app.fileManager.renameFile(currentFile, newPath);
}

const workTitle = `${author} 『${title}』`;
const lines = [
  `---`,
  `제목: ${title}`,
  `저자: ${author}`,
  `분야: ${field}`,
  `상태: ${status}`,
  `페이지수: ${pageCount}`,
  `평가: ${rating}`,
  `발행일: ${published}`,
  `읽기 시작한 날짜: ${startedReading}`,
  `완독 날짜: ${finishedReading}`,
  `---`,
  ``,
  `### ${workTitle} 소설의 줄거리는`,
  `- 이 이야기는 ...`,
  ``,
  `### ${workTitle} 서평`,
  `- 스포없는 서평`,
  ``,
  `### ${workTitle} 후감`,
  `- 스포를 담은 감상`,
];

tR += lines.join("\n");
_%>
