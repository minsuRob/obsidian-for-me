<%*
let author = tp.frontmatter["저자"] ?? "";
let title = tp.frontmatter["제목"] ?? "";
let published = tp.frontmatter["발행일"] ?? "";

if (!author) {
  author = await tp.system.prompt("저자") ?? "";
}

if (!title) {
  title = await tp.system.prompt("제목") ?? "";
}

const fileName = title
  .replace(/[\\/:*?"<>|#^[\]]/g, "")
  .replace(/\s+/g, " ")
  .trim();
const currentFile = app.workspace.getActiveFile();
if (fileName && currentFile && currentFile.basename !== fileName) {
  const folder = currentFile.parent?.path ?? "";
  const newPath = `${folder ? folder + "/" : ""}${fileName}.md`;
  await app.fileManager.renameFile(currentFile, newPath);
}

const workTitle = `${author} 『${title}』`;
const lines = [
  `---`,
  `저자: ${author}`,
  `제목: ${title}`,
  `발행일: ${published}`,
  `---`,
  ``,
  `### ${workTitle} 소설의 줄거리는`,
  `- 이 이야기는 ...`,
  ``,
  `### ${workTitle} 서평`,
  `- 스포없는 서평`,
  ``,
  `### ${workTitle} 서평`,
  `- 스포를 담은 감상`,
];

tR += lines.join("\n");
_%>
